# Pro Timer  (Productivity Timer)

- This just displays a full screen pi chart timer. 
- Default value is 25min but custom value can be specified as a hash in minutes
- Double click anywhere and the timer restarts.